import AdminAccountsClient from "@/components/admin/admin-accounts-client";

export const metadata = {
  title: "Admin Accounts — Admin — Property Fifth",
};

export default function AdminAccountsPage() {
  return <AdminAccountsClient />;
}
